# Browser-safe Supabase values. Copy to .env and obtain them from `npx supabase status`
# for local development or the hosted project's Connect dialog.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Never put a secret key or service-role key in a PUBLIC_* variable.
